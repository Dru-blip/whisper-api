/**
 * REDIS_OTP_PREFIX is the prefix used for OTP keys in Redis.
 */
export const REDIS_OTP_PREFIX = 'otp:';

/**
 * OTP_EXPIRY is the time in seconds after which an OTP expires.
 */
export const OTP_EXPIRY = 300;
